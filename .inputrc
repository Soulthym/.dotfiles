set editing-mode vi
# vi settings
$if mode=vi
    # normal mode
    set keymap vi-command
    "gg": beginning-of-history
    "G": end-of-history
    # insert mode
    set keymap vi-insert
    C-l: clear-screen
$endif
set mark-symlinked-directories on
